;; (ns faiz.init
;;   (:gen-class)
;;   (:require [clojure.tools.nrepl.server :as nrepl]
;;             [faiz.repl :as s]))

;; (defn init-server
;;   []
;;   (nrepl/start-server :port 7053)
;;   (s/start-server))

;; ;; (defn -main
;; ;;   "Application entry point"
;; ;;   [& args]
;; ;;   (init))
